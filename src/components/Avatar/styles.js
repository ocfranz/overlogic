import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    avatar: {
        height : 100,
        width: 100,
        borderRadius : 100,
        alignItems : 'center'
    }
  });

  export default styles;